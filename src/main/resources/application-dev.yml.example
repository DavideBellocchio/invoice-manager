spring:
  datasource:
    url: jdbc:postgresql://localhost:5056/your_db_name
    username: your_db_username
    password: your_db_password
  config:
    activate:
      on-profile: dev
  jpa:
    hibernate:
      ddl-auto: create-drop